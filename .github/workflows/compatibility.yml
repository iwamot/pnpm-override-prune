name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-node.yml@7b3c075718bd3e57a5316384ef1184ab931ef847 # v3.2.0
    with:
      node-versions: '["20","22","24"]'
