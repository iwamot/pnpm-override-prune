name: Compatibility

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-node.yml@866cf6089ddb88af6b479a14a4a78910b472b5f6 # v8.11.0
    with:
      # The floor is the oldest Node release upstream still supports, and
      # `engines.node` in package.json carries the same number: that field is
      # what promises the package installs there. Both move when a release
      # leaves support.
      node-versions: '["22","24"]'
