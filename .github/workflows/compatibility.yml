name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-node.yml@afdf21101495a5ec5df69338e8f94ed6ee8cf32d # v3.4.0
    with:
      node-versions: '["20","22","24"]'
