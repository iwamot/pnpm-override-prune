name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-node.yml@7e1e1b73633f829ea0ac1ca934aa4d085561d61a # v7.1.0
    with:
      node-versions: '["20","22","24"]'
